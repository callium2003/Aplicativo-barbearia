"use client";
import {createClient} from"@supabase/supabase-js";import{FormEvent,useEffect,useState}from"react";
const s=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY);type P={id:string;name:string};type B={id:string;weekday:number;starts_at:string;ends_at:string};const ds=["Dom","Seg","Ter","Qua","Qui","Sex","Sáb"];
export default function Profissionais(){const[p,setP]=useState<P[]>([]),[id,setId]=useState(""),[breaks,setBreaks]=useState<B[]>([]),[msg,setMsg]=useState("Carregando...");async function load(){const{data:{user}}=await s.auth.getUser();if(!user)return window.location.replace("/entrar");const{data}=await s.from("barbershops").select("id").eq("owner_id",user.id).single();const{data:ps}=await s.from("professionals").select("id,name").eq("barbershop_id",data?.id||"").eq("active",true);setP(ps||[]);setMsg("");}async function choose(v:string){setId(v);const{data}=await s.from("professional_breaks").select("id,weekday,starts_at,ends_at").eq("professional_id",v);setBreaks(data||[])}useEffect(()=>{void load()},[]);async function save(e:FormEvent){e.preventDefault();if(!id)return;const f=new FormData(e.currentTarget);const weekday=Number(f.get("weekday"));const values={professional_id:id,weekday,starts_at:String(f.get("start")),ends_at:String(f.get("end"))};const{error}=await s.from("professional_breaks").upsert(values,{onConflict:"professional_id,weekday"});setMsg(error?"Não foi possível salvar.":"Intervalo salvo.");if(!error)await choose(id)}async function block(e:FormEvent){e.preventDefault();if(!id)return;const f=new FormData(e.currentTarget);const{error}=await s.from("professional_time_blocks").insert({professional_id:id,starts_at:new Date(String(f.get("start"))).toISOString(),ends_at:new Date(String(f.get("end"))).toISOString(),reason:String(f.get("reason")||"")});setMsg(error?"Não foi possível bloquear.":"Bloqueio pontual salvo.");}return <main style={{padding:24,fontFamily:"Arial",background:"#f6f2ed",minHeight:"100vh"}}><a href="/painel/configurar">← Configurações</a><h1>Intervalos e bloqueios</h1><p>Sem intervalo cadastrado, todo o expediente do profissional fica disponível.</p><select value={id} onChange={e=>void choose(e.target.value)} style={{padding:10,width:"100%"}}><option value="">Escolha o profissional</option>{p.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select>{id&&<><form onSubmit={save} style={{marginTop:18,background:"white",padding:16}}><b>Intervalo semanal</b><div style={{display:"flex",gap:8,marginTop:10,flexWrap:"wrap"}}><select name="weekday">{ds.map((d,i)=><option key={d} value={i}>{d}</option>)}</select><input name="start" type="time" required/><input name="end" type="time" required/><button>Salvar intervalo</button></div>{breaks.map(x=><small key={x.id} style={{display:"block",marginTop:8}}>{ds[x.weekday]}: {x.starts_at.slice(0,5)}–{x.ends_at.slice(0,5)}</small>)}</form><form onSubmit={block} style={{marginTop:18,background:"white",padding:16}}><b>Bloqueio pontual</b><div style={{display:"grid",gap:8,marginTop:10}}><input name="start" type="datetime-local" required/><input name="end" type="datetime-local" required/><input name="reason" placeholder="Ex.: almoço, folga ou férias"/><button>Bloquear período</button></div></form></>}{msg&&<p>{msg}</p>}</main>}
